#include<stdio.h>
#include<conio.h>
#include<string.h>
void main()
{
char str[10];
int len;
printf("\n enter the string");
scanf("%s",&str);
len=strlen(str);
printf("\n the length of string is%d",len);
}
